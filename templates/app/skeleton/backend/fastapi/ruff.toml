line-length = 100
target-version = "py312"

[lint]
select = ["E", "F", "I", "N", "UP", "B", "SIM"]
ignore = ["B008"]  # FastAPI Depends() must be called in argument defaults

[format]
quote-style = "double"
indent-style = "space"
